[package]
name = "planet-terrain"
version.workspace = true
edition.workspace = true
description = "One continuous field over the sphere, read per point by the picture and per territory by the model. Knows nothing about the tessellation."

[dependencies]
planet-model.workspace = true
sphere-tessellation.workspace = true
