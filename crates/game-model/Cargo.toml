[package]
name = "game-model"
version.workspace = true
edition.workspace = true
description = "The game: a state, a transition, and the one function between them. Integers only, engine-free."

[dependencies]
