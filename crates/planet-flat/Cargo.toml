[package]
name = "planet-flat"
version.workspace = true
edition.workspace = true
description = "The Bevy adapter for the flat projection: a window driving the software rasterizer, and the shader that mirrors it."

[dependencies]
bevy = { version = "0.19", default-features = false, features = ["2d", "3d", "ui"] }
planet-bevy.workspace = true
planet-ecs.workspace = true
planet-model.workspace = true
planet-raster.workspace = true
planet-render.workspace = true
