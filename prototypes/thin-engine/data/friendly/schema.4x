# The shared schema: every relation, its columns in order, and what points at what.
#
# **Every reference is to a single opaque integer id** - Sean, 2026-09-15: *I want the truth of the
# data model to be fully normalized, which I believe means always referencing by ids, and a single
# id at that.*
#
# **Sixteen relations are keyed by such an id and one is not.** `residency` carries a `quantity`
# and is keyed by its description, `(what, where)` - Sean, 2026-09-15: *it would make no sense to
# have both an id and a quantity in the same logical model.* **Nothing references a residency**,
# which is what lets it do without one.
#
# **The ids mean nothing and are not meant to be read.** Making them legible is the user-facing
# style's job, not this one's. What is legible here is the `name` column, which is decoration -
# with two exceptions worth knowing: a row names its relation by `relation.name`, and a row's
# values are keyed by `column.name`, so those two decorations are what the notation is written in.


## Every relation, and its columns in order

{relation id:1 name:relation}
{relation id:2 name:column}
{relation id:3 name:reference}
{relation id:4 name:state}
{relation id:5 name:role}
{relation id:6 name:rule}
{relation id:7 name:input}
{relation id:8 name:clause}
{relation id:9 name:binding}
{relation id:12 name:primitive}
{relation id:13 name:territory}
{relation id:15 name:adjacency}
{relation id:17 name:literal}
{relation id:18 name:deposit}
{relation id:19 name:extractor}
{relation id:20 name:limit}
{relation id:21 name:reading}
{relation id:22 name:attribute}
{relation id:23 name:relation-of}
{relation id:24 name:family}
{relation id:25 name:member}
{relation id:26 name:unit}
{relation id:27 name:resource}
{relation id:28 name:scout}
{relation id:29 name:labor}
{relation id:30 name:metal}
{relation id:31 name:food}
{relation id:33 name:supply}
{relation id:34 name:provides}
{relation id:35 name:transport}
{relation id:36 name:consumes}
{relation id:37 name:assigns}
{relation id:38 name:trait}
{relation id:39 name:carries}
{relation id:40 name:part}
{relation id:41 name:argument}


## The columns

{column id:1 relation:relation seq:1 name:id}
{column id:2 relation:relation seq:2 name:name}
{column id:3 relation:column seq:1 name:id}
{column id:4 relation:column seq:2 name:relation}
{column id:5 relation:column seq:3 name:seq}
{column id:6 relation:column seq:4 name:name}
{column id:7 relation:reference seq:1 name:id}
{column id:8 relation:reference seq:2 name:column}
{column id:9 relation:reference seq:3 name:to}
{column id:10 relation:state seq:1 name:id}
{column id:11 relation:state seq:2 name:relation}
{column id:12 relation:role seq:1 name:id}
{column id:13 relation:role seq:2 name:name}
{column id:14 relation:rule seq:1 name:id}
{column id:15 relation:rule seq:2 name:name}
{column id:16 relation:input seq:1 name:id}
{column id:17 relation:input seq:2 name:rule}
{column id:18 relation:input seq:3 name:seq}
{column id:19 relation:input seq:4 name:name}
{column id:20 relation:input seq:5 name:of}
{column id:21 relation:clause seq:1 name:id}
{column id:22 relation:clause seq:2 name:rule}
{column id:23 relation:clause seq:3 name:seq}
{column id:24 relation:clause seq:4 name:role}
{column id:25 relation:clause seq:5 name:relation}
{column id:26 relation:binding seq:1 name:id}
{column id:27 relation:binding seq:2 name:clause}
{column id:28 relation:binding seq:3 name:column}
{column id:29 relation:binding seq:4 name:input}
{column id:36 relation:primitive seq:1 name:id}
{column id:37 relation:primitive seq:2 name:word}
{column id:38 relation:territory seq:1 name:id}
{column id:41 relation:adjacency seq:1 name:id}
{column id:42 relation:adjacency seq:2 name:from}
{column id:43 relation:adjacency seq:3 name:to}


{column id:47 relation:literal seq:1 name:id}
{column id:48 relation:literal seq:2 name:clause}
{column id:49 relation:literal seq:3 name:column}
{column id:50 relation:literal seq:4 name:value}

{column id:51 relation:deposit seq:1 name:where}
{column id:52 relation:deposit seq:2 name:what}
{column id:53 relation:deposit seq:3 name:density}
{column id:54 relation:deposit seq:4 name:quantity}
{column id:55 relation:extractor seq:1 name:where}
{column id:56 relation:extractor seq:2 name:what}
{column id:101 relation:extractor seq:3 name:working}
{column id:57 relation:extractor seq:4 name:quantity}
{column id:58 relation:limit seq:1 name:held}
{column id:59 relation:limit seq:2 name:by}

{column id:60 relation:reading seq:1 name:id}
{column id:61 relation:reading seq:2 name:clause}
{column id:62 relation:reading seq:3 name:column}
{column id:63 relation:reading seq:4 name:of}
{column id:64 relation:reading seq:5 name:takes}

{column id:65 relation:attribute seq:1 name:column}
{column id:66 relation:attribute seq:2 name:relation}

{column id:67 relation:relation-of seq:1 name:clause}
{column id:68 relation:relation-of seq:2 name:input}
{column id:69 relation:family seq:1 name:relation}
{column id:70 relation:member seq:1 name:kind}
{column id:71 relation:member seq:2 name:family}


## The families, and the kinds that belong to them
#
# **A family is an abstract relation: columns and no rows.** The columns are the shape its members
# share, and `{member ...}` is the set - Sean, 2026-09-17, having considered an exists/not-exists
# trait and a set of kinds and asking whether both were relevant: they are the same mechanism.
# `spec/data/families.4x` writes it this way already, and `spec/data/for.4x` uses it for exactly
# what it is for here: `{for block:build-extractor seq:3 kind:$resource}`.

{column id:72 relation:unit seq:1 name:where}
{column id:98 relation:unit seq:2 name:moving}
{column id:73 relation:unit seq:3 name:quantity}
{column id:74 relation:resource seq:1 name:where}
{column id:75 relation:resource seq:2 name:quantity}


## Every kind of thing, as a relation of its own
#
# **Sean, 2026-09-17**, looking at a deposit, an extractor and a labor in one territory: *I notice
# that in territory-1, we have a deposit, an extractor, and a food. Yet the way we specify this is
# different.* **It was: two of them had their kind in the schema and one had it in a column.** A
# kind is a relation now, and `residency` and `thing` are both gone.
#
# **This is `spec/data/carries.4x` said in the notation** - a kind and the traits it carries is a
# relation and its columns.

{column id:76 relation:scout seq:1 name:where}
{column id:99 relation:scout seq:2 name:moving}
{column id:77 relation:scout seq:3 name:quantity}
{column id:78 relation:labor seq:1 name:where}
{column id:79 relation:labor seq:2 name:quantity}
{column id:80 relation:metal seq:1 name:where}
{column id:81 relation:metal seq:2 name:quantity}
{column id:82 relation:food seq:1 name:where}
{column id:83 relation:food seq:2 name:quantity}


## How many times a thing may still act
#
# **Sean, 2026-09-17**, on an extractor that can be worked as often as there is labor for it:
# *rediness is going to end up being a number of times we can do something rather than whether we
# have done something.* **So it is a quantity, and a quantity is a relation's.**
#
# **An allowance is its own counted relation rather than a column on the thing.** A column would
# have meant going from `working:3` to `working:2`, which is arithmetic on a named column and
# something the engine cannot do; a quantity is arithmetic it already does, and running out is the
# refusal it already gives. **What that gives up is which extractor was worked** - and two
# extractors with one work each cannot be told from two extractors sharing two works by anything
# the game can ask, because extractors are fungible.
#
# **One relation per kind of readiness**, named for the activity, which is what `spec/data/traits.4x`
# does with `working`, `moving`, `laboring` and `bearing`. Each is keyed like the thing that has
# it, so `{limit ...}` says an allowance cannot exceed the things holding it.

{column id:102 relation:assigns seq:1 name:id}
{column id:103 relation:assigns seq:2 name:clause}
{column id:104 relation:assigns seq:3 name:input}
{column id:105 relation:assigns seq:4 name:value}
{column id:106 relation:trait seq:1 name:id}
{column id:107 relation:trait seq:2 name:name}
{column id:108 relation:carries seq:1 name:kind}
{column id:109 relation:carries seq:2 name:trait}
{column id:110 relation:part seq:1 name:id}
{column id:111 relation:part seq:2 name:of}
{column id:112 relation:part seq:3 name:is}
{column id:113 relation:part seq:4 name:seq}
{column id:114 relation:argument seq:1 name:id}
{column id:115 relation:argument seq:2 name:part}
{column id:116 relation:argument seq:3 name:input}
{column id:117 relation:argument seq:4 name:value}


## What a place has room for, and what each kind takes up
#
# **Sean, 2026-09-17**: *I am thinking of inventing a resource that vehicles take up and having a
# certain limit per territory that is the same across all territories. A lot of real time strategy
# games do this, and I know the board game twilight imperium does this as well.*
#
# **The allowance is a constant and the rate is per kind**, which is the half `spec/data/limit.4x`
# cannot write: `{limit container:territory contained:ark n:2}` counts arks, and a transport worth
# two scouts has nowhere to be said.
#
# **A pool names the kind of place it is an allowance of**, and a drawing kind's place is whichever
# of its columns points at that - so the engine finds it rather than knowing the game calls it
# `where`.

{column id:87 relation:supply seq:1 name:id}
{column id:88 relation:supply seq:2 name:name}
{column id:89 relation:supply seq:3 name:per}

{column id:90 relation:provides seq:1 name:kind}
{column id:91 relation:provides seq:2 name:what}
{column id:92 relation:provides seq:3 name:quantity}

{column id:93 relation:consumes seq:1 name:kind}
{column id:94 relation:consumes seq:2 name:what}
{column id:95 relation:consumes seq:3 name:quantity}

{column id:96 relation:transport seq:1 name:where}
{column id:100 relation:transport seq:2 name:moving}
{column id:97 relation:transport seq:3 name:quantity}


## What points at what
#
# **A reference is always to a relation's `id`.** The engine checks that such a row is there, so a
# value pointing at nothing is refused when the data is read rather than when it is used.

{reference id:1 column:4 to:relation}
{reference id:2 column:8 to:column}
{reference id:3 column:9 to:relation}
{reference id:4 column:11 to:relation}
{reference id:5 column:17 to:rule}
{reference id:6 column:20 to:relation}
{reference id:7 column:22 to:rule}
{reference id:8 column:24 to:role}
{reference id:9 column:25 to:relation}
{reference id:10 column:27 to:clause}
{reference id:11 column:28 to:column}
{reference id:12 column:29 to:input}
{reference id:16 column:42 to:territory}
{reference id:17 column:43 to:territory}


{reference id:20 column:48 to:clause}
{reference id:21 column:49 to:column}

{reference id:22 column:51 to:territory}
{reference id:23 column:52 to:resource}
{reference id:24 column:55 to:territory}
{reference id:25 column:56 to:resource}
{reference id:26 column:58 to:relation}
{reference id:27 column:59 to:relation}

{reference id:28 column:61 to:clause}
{reference id:29 column:62 to:column}
{reference id:30 column:63 to:clause}
{reference id:31 column:64 to:column}

{reference id:32 column:65 to:column}
{reference id:33 column:66 to:relation}

{reference id:34 column:67 to:clause}
{reference id:35 column:68 to:input}
{reference id:36 column:69 to:relation}
{reference id:37 column:70 to:relation}
{reference id:38 column:71 to:relation}

{reference id:41 column:76 to:territory}
{reference id:42 column:78 to:territory}
{reference id:43 column:80 to:territory}
{reference id:44 column:82 to:territory}

{reference id:45 column:103 to:clause}
{reference id:46 column:104 to:input}

{reference id:47 column:89 to:relation}
{reference id:48 column:90 to:relation}
{reference id:49 column:91 to:supply}
{reference id:50 column:93 to:relation}
{reference id:51 column:94 to:supply}
{reference id:52 column:96 to:territory}
{reference id:53 column:108 to:relation}
{reference id:54 column:109 to:trait}
{reference id:55 column:111 to:rule}
{reference id:56 column:112 to:rule}
{reference id:57 column:115 to:part}
{reference id:58 column:116 to:input}


## Which relations are the game's state
#
# **This is what bounds the comparison.** `expected.4x` states the state after, and a diff has to
# know which relations to look at - without this it would compare the rule and the command too.

{state id:1 relation:territory}
{state id:3 relation:adjacency}
{state id:5 relation:deposit}
{state id:6 relation:extractor}
{state id:7 relation:scout}
{state id:8 relation:labor}
{state id:9 relation:metal}
{state id:10 relation:food}
{state id:12 relation:transport}
{state id:13 relation:provides}
{state id:14 relation:consumes}


## Which relations are families, and who belongs
#
# **A `unit` may be moved and a `resource` may be extracted**, and those are the two sets any rule
# here ranges over. **`move` is typed `of:unit`**, so a deposit is not offered to it because the
# game says a deposit is not a unit - rather than because `move`'s add clause happens to leave a
# column unbound, which is how it would have been excluded without this.

{family relation:unit}
{family relation:resource}

{member kind:scout family:unit}
{member kind:transport family:unit}
{member kind:metal family:resource}
{member kind:food family:resource}


## What a deposit's density is, and what a deposit holds
#
# **A density is a fact about a deposit and not part of what a deposit is.** Sean, 2026-09-17,
# ruling out the alternative: a territory may have deposits of two grades, or the notation may be
# uniform and minimal, and it cannot have both - *we are dropping (2)*. `spec/planet.md` asks for
# one: *For each resource, a territory has capacity for some number of extractors, and a density
# that each of them yields.*
#
# **So a second grade is two rows with one key and the structure refuses it**, which is the whole
# reason the column is marked: with density in the key it would be a legal state nothing could
# name.

{attribute column:53 relation:deposit}
{attribute column:66 relation:attribute}
{attribute column:68 relation:relation-of}


# **An extractor stands in a deposit, so there cannot be more of them than there are deposits.**
# Sean: *We can't place an extractor if there are no available deposits.* No rule says so - every
# rule already refuses the world it would leave, so this binds rules that do not exist yet.

{limit held:extractor by:deposit}


## What a supply is, and what it is measured in
#
# **How much is provided and how much is consumed are rows of the world**, stated in each test's
# `given` - Sean, 2026-09-17: *there are certain things I always want to see in tests because I
# need to compute the tests in my head.* **A layer that kept them out of a scenario would be a
# layer hiding what the test is about**, and he ruled that comprehension wins where the two
# conflict.
#
# **This declares the supply and nothing else.** `per` says what it is measured in, so the check
# does not have to work out which relation every provider and consumer has in common.

{supply id:1 name:berth per:territory}


## The three roles a clause can have
#
# **The engine branches on the `name`, not on the id.** An id is arbitrary, so a rule that says
# `role:2` means nothing until the row is looked up - which makes `role.name` load-bearing where
# every other `name` is decoration.

{role id:1 name:require}
{role id:2 name:remove}
{role id:3 name:add}
{role id:4 name:put}


## What a trait is, and which kinds carry one

# **A trait is an allowance a kind carries**, and it is the column that holds how many are left.
# `moving` is one and `working` is another; a scout has a `moving` and an extractor has a
# `working`, and `refresh` is one rule over both because the trait is an argument rather than
# part of the rule's name.
#
# **Sean, 2026-09-18**: *I don't like that I have a separate refresh command for each resource, I
# feel like this should be parameterized somehow.* And on what the parameterisation has to reach:
# *I should be able to declare separate things with separate commands, as well as explicitly
# declare group commands. So i can refresh a scout, refresh a transport, refresh all movable,
# refresh all workable, and their intersections.*
#
# **`carries` is checked both ways**, which is what stops it drifting from the columns it
# describes: a kind that carries a trait declares a column of that name, and a column named for a
# trait is carried by the relation that declares it. Neither half is derivable from the other -
# the first would let a column go undeclared, the second would let a `carries` row name nothing -
# so the check is a pair and `src/schema.rs` states it as one.
#
# **A family carries what its members carry.** `unit` declares `moving` because `scout` and
# `transport` do, so `{refresh what:unit trait:moving}` is the group command and
# `{refresh what:scout trait:moving}` is the separate one. That is Sean's *their intersections*
# with no third mechanism: the pair of arguments is the intersection.

{trait id:1 name:moving}
{trait id:2 name:working}

{carries kind:unit trait:moving}
{carries kind:scout trait:moving}
{carries kind:transport trait:moving}
{carries kind:extractor trait:working}


## What a composite is, and why the tree is the point

# **A rule is a leaf or a composite, and a composite names rules in order.** `{part id:1
# of:end-turn is:refresh seq:1}` says ending a turn does a refresh first; `{argument id:1 part:1
# input:what value:unit}` says which refresh.
#
# **It is the clause layer one level up, and deliberately the same shape.** A clause has an id and
# `{binding ...}` rows name it; a part has an id and `{argument ...}` rows name it. Nothing here
# is a new idea about how a rule is described - it is the existing idea applied to rules rather
# than to rows.
#
# **Sean, 2026-09-18**, on what the structure has to be: *Whatever it turns out to mean, it must be
# able to organize the entirety of game rules is some type of acyclic graph or tree. Otherwise it
# will be impossible for a human player to understand how to play the game.* So `src/schema.rs`
# refuses a cycle and refuses a rule with two parents, and `examples/tree.rs` prints the whole of
# it on one screen.
#
# **A rule with one parent may still be named twice by it.** `end-turn` refreshes twice, once per
# trait, and those are two steps of one order rather than two parents - which is what a person
# reading the tree sees and what the check counts.
#
# **The roots are the player's menu.** A rule that is somebody's part is fired by that somebody; a
# rule that is nobody's part is offered. So nothing declares an owner: `spec/data/block.4x` says
# `owner:world` and here the tree says it, which is one fact in one place rather than two that can
# disagree.
#
# **Sean, 2026-09-18**, on why a part is still a command in its own right: *Why can't refresh be
# both a player command and part of the turn. It seems it will make it easier to read tests across
# multiple turns if i see an end turn command, and it will be easier to test the end turn command
# itself if i can test its parts.* **Fired by name and offered to a player are two questions**, and
# this lane had collapsed them into one.
