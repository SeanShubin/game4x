# The orchestrator: what this test does, in the order it does it.
#
# **A test is data too.** Nothing here is Rust - the harness reads this file, and every step is a
# row. `seq` orders them across the four step relations, and is read as a number rather than as
# text, because `10` sorts before `2`.

{test name:the-scout-moves-to-an-adjacent-place}

{load file:script.4x into:1}
{load file:schema.4x into:2}
{load file:engine.4x into:2}
{load file:rules.4x into:2}
{load file:before.4x into:2}
{load file:command.4x into:2}
{load file:expected.4x into:3}

{execute command:move}
{compare this:4 with:3}
{report title:the-first-test}
