# The command: move residency 1 - the scout, in territory 1 - to territory 2.
#
# **Every value is an id.** `input:1` is `it`, and its value `1` is the residency; `input:2` is
# `what` and its value is the thing. Nothing here is readable without the schema, which is what
# the user-facing style is for.

{command id:1 rule:1}

{argument id:1 command:1 input:1 value:1}
{argument id:2 command:1 input:2 value:1}
{argument id:3 command:1 input:3 value:1}
{argument id:4 command:1 input:4 value:2}
