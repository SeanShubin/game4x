[package]
name = "planet-view"
version.workspace = true
edition.workspace = true
description = "Prototype: a sphere fanned out flat. Composition root only."

[dependencies]
planet-render.workspace = true
planet-bevy.workspace = true
planet-ecs.workspace = true
bevy = { version = "0.19", default-features = false, features = ["2d"] }
png = "0.18"
